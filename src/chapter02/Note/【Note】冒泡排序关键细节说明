关键细节说明
--为什么外层循环是 n-1 轮？
    数组有 n 个元素，只需确定前 n-1 个元素的位置，最后 1 个元素自然处于正确位置。

--为什么内层循环是 n-1-i？
    每轮结束后，末尾 i 个元素已排好序，无需重复比较，减少循环次数。
--稳定性说明：
    冒泡排序是稳定排序（相等元素的相对位置不变），因为只有arr[j] > arr[j+1]时才交换，相等时不交换。