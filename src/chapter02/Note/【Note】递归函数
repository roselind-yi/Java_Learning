1.
递归函数指的是自己调用自己，以达到某种目的
函数将参数传给下一个自己，并将下一个自己的返回作为自己的返回

2.递归常见错误：
未写递归出口，递归会变成死循环。
会造成栈的溢出，Java的（执行栈）的容量默认10000个左右的栈帧。
递归出口必须写在函数体的开头，通常是满足某种条件时return普通结果。

return 普通结果 ====结束递归
return 函数调用结果 ====继续递归

用递归解决“斐波拉契数列”的问题：
1 1 2 3 5 8 13 21 33 54 ...