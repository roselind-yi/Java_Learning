1.函数的组成部分
【访问权限修饰符】  |--【静态修饰父】  返回值类型    函数名（参数列表）{函数体}

|1------【访问权限修饰符，决定了函数可调用的范围】
public:公有权限，此函数可以跨类、挎包调用
private:私有权限，此函数不可以跨类调用，只能本类自己调用
省略不写：默认权限，此函数不可以跨类调用，只能本包自己调用

|2------【静态修饰符，决定了函数的调用方式】
static:静态函数，直接用函数名调用，跨类调用时在函数名前加类名
省略不写：非静态函数，需要创建对象后调用（以后学面向对象再说）

|3------【返回值类型，决定了函数运行结束后，返回值的类型】
void：空的类型，表示此函数没有返回值
具体类型：如int,double,String,boolean,int[]

|4------【函数名，根据函数的功能自定义】

|5------【参数列表，根据函数的功能自定义】
可以是无参（空参）函数，（）里面什么都不写。
foo()
foo(int x)
foo(int[] arr,int y)

|6------【函数体】
函数的实现过程
函数执行的指令集合，执行指令执行完，函数自动返回。
或用return手动返回。
若函数定义了返回值类型，函数运行完必须由return语句
若函数返回值类型是void，则可无return。




