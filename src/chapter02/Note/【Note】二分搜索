一、二分搜索（迭代版 + 递归版）核心实现步骤
通用前提
二分搜索的核心要求：数组必须是有序的（升序 / 降序，以下以升序为例），否则无法使用。
1. 迭代版二分搜索核心步骤（5 步）
步骤    具体操作
1    初始化边界：左指针 left = 0，右指针 right = 数组长度-1，结果索引 index = -1（默认未找到）
2    循环条件：left <= right（左右指针未重合 / 交叉，说明还有元素可查）
3    计算中间索引：mid = left + (right - left)/2（避免 (left+right)/2 整数溢出）
4    比较中间值与目标值：
① 中间值 > 目标值：目标在左半区，右指针左移 right = mid - 1
② 中间值 < 目标值：目标在右半区，左指针右移 left = mid + 1
③ 中间值 = 目标值：找到目标，记录索引 index = mid，终止循环
5    循环结束后返回 index（找到则为对应索引，未找到则为 - 1）
-----------------------------------------------------------------------------------------------
2. 递归版二分搜索核心步骤（5 步）
步骤    具体操作
1    递归终止条件 1（未找到）：left > right（指针交叉，无元素可查），返回 - 1
2    计算中间索引：mid = left + (right - left)/2（同迭代版，避免溢出）
3    比较中间值与目标值：
① 中间值 > 目标值：递归查询左半区，参数为 (arr, target, left, mid-1)
② 中间值 < 目标值：递归查询右半区，参数为 (arr, target, mid+1, right)
③ 中间值 = 目标值：递归终止条件 2（找到），返回 mid
4    递归调用自身，传递新的左右指针边界
5    最终返回递归结果（找到则为索引，未找到则为 - 1）
易错点提醒：递归版初始调用需传 left=0、right=arr.length-1；递归终止条件是 left > right（而非 left >= right，否则会漏掉最后一个元素）。