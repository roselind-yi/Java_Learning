核心结论
最佳缓冲区大小：64KB ~ 256KB通用推荐直接用：128KB（131072 字节）不建议小于 4KB，不建议大于 1MB。

---
关键原因
1. 为什么不设太小（比如 1KB、4KB）
- 缓冲区太小：系统调用次数暴增，read/write 频繁切换用户态内核态，IO 开销拉满，速度极慢。
2. 为什么不设太大（比如 4MB、10MB、直接一次性读 1G）
1. 占用堆内存过高，1G 文件开超大缓冲区会白白占内存，无收益；
2. 操作系统页缓存、磁盘 IO 单次读写最优块 本身就不需要太大；
3. 超大缓冲区不会提升速度，反而内存浪费、GC 压力变大。
3. 为什么 64K/128K/256K 最优
- 机械硬盘、SSD 单次 IO 批量读写最优区间 就是 64K~256K；
- 匹配操作系统文件系统块、页缓存默认配置；
- 系统调用次数适中，内存占用极小，吞吐拉满。