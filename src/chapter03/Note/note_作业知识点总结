知识点总结

1. 包装类型 vs 基本类型

• 包装类型（如 Integer 、 Double ）：是对象，可以为 null ，适合需要表示"无值"的场景

• 基本类型（如 int 、 double ）：不是对象，有默认值（ int 默认0， double 默认0.0）

• 什么时候用包装类型：在类的成员变量中、集合框架中、需要表示 null 值时

2. 构造函数的作用

• 构造函数是创建对象时自动调用的方法

• 用于初始化对象的成员变量

• 可以重载，提供不同的初始化方式

• 如果没有定义构造函数，Java会自动生成一个无参构造函数

3. 静态方法 vs 成员方法

特性 静态方法 成员方法

调用方式 类名.方法名( ) 对象名.方法名( )

访问成员变量 只能访问静态成员变量 可以访问所有成员变量

this 关键字 不能使用 可以使用

4. 方法重载的规则

• 方法名必须相同

• 参数列表必须不同（参数类型、数量、顺序至少有一个不同）

• 返回类型可以相同也可以不同（不影响重载）

5. this 关键字的用法

• 表示当前对象

• 用于区分成员变量和参数（如 this.id = id ）

• 可以调用当前对象的其他方法（如 this.print( )）
