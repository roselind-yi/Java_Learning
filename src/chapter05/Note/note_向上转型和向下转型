
【第一层含义】：基本类型【小类型】和【大类型】互转
小类型转大类型【向上】：自动转型
大类型转小类型【向下】：需要加（类型）进行强制转型
byte<->int
int<->float

【第二行含义】：【子类类型】和【父类类型】之间的转换
子类转父类【向上】：自动转型
父类转子类【向下】：需要加（类型）强制转型

【向上转型】的应用
【类型多态】
参数多态：  【实参】 会【自动】【向上转型】为【形参】的类型
返回值多态：【返回的对象】会【自动】【向上转型】为【返回值】的类型
数组多态:  【数组中存储的对象】会【自动】【向上转型】为【数组】的类型

【方法多态】
向上转型后，用父类的类型调用方法，如pet.show();
此时，有多个类里面都有show()方法【父类是基础的show()方法，子类是重写的show()方法】
编译时，编译器不会决定到底与哪个show方法的代码进行绑定。
运行时，根据pet引用指向的原型对象决定调用哪个类的show()方法。

【向下转型】的应用
向下转型的前置条件一定是先发生向上转型，即【子类对象】已经转为【父类类型】
父类类型可以访问子类【重写的类型】
父类类型无法访问子类的【特有功能】，若需要调用，此时需要用到的向下转型(即用其原始进行操作)
